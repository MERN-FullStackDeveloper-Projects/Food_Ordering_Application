export const config = {
    serverBaseUrlUser: 'http://localhost:4000',
    serverBaseUrlCatalog: 'http://localhost:4001',
    serverBaseUrlOrder:'http://localhost:40003'

    // later will do it by using API gateway
}